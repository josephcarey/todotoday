ToDo = require("./todo");

describe("getAllToDos():", () => {
  test("true === true", () => {
    expect(true).toBe(true);
  });
});
describe("addToDo(toDoText):", () => {
  test("true === true", () => {
    expect(true).toBe(true);
  });
});
describe("deleteToDo(idToDelete):", () => {
  test("true === true", () => {
    expect(true).toBe(true);
  });
});
describe("editToDoText(idToEdit, newText):", () => {
  test("true === true", () => {
    expect(true).toBe(true);
  });
});
